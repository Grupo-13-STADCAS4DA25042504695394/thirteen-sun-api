#ENVIRONMENT DEV VARIABLES
#NOTE: never commit real credentials to version control. Use placeholders or environment variables instead.

POSTGRES_USER=sun_api_user
POSTGRES_PASSWORD=sun@password
POSTGRES_DB=sun_api_db